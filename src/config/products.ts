import type { Product } from './product-schema';
/** Before publishing, review every product policy against the app's actual SDKs and Google Play Data Safety declaration. */
export const products: readonly Product[] = [];
